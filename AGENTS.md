# Agent Instructions

이 저장소에서 작업하는 모든 에이전트는 작업 전에 루트의 `CLAUDE.md`를 처음부터 끝까지 읽고
그 지침을 따른다.

특히 다음 원칙은 생략할 수 없다.

- 삼성SDS Nexplant MES의 공개 사실과 이 프로젝트의 자체 구현을 구분한다.
- 실제로 수행하지 않은 실험, 측정값, 기술 선택, 장애 복구 결과를 만들지 않는다.
- Baseline → 문제 측정 → 원인 분석 → 대안 비교 → 구현 → 동일 조건 재측정 → 회귀 테스트 순서를 따른다.
- 모든 REST 요청을 메시지 큐로 보내지 않고 동기 Command와 비동기 Event의 경계를 먼저 설계한다.
- 주요 결과를 Git의 Experiment/Decision 문서와 연결된 Notion 기록에 남긴다.

